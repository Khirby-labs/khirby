# docs/journal/devlog/2026-07-24-web-boundary-testing.md
Issue:    none (ad-hoc audit → plan, driven by direct prompts) · branch worktree-web-p0-msw-specs
Goal:     make apps/web tests measure real capabilities at system boundaries, not mirror our own code; close the coverage vacuum the audit found
Done:     P0–P3 on one branch (MR !11). MSW test foundation (src/test/*); specs for pipeline.store, useRealtimeEvents, router guard; component specs LoginView/AddLeadModal + RolesView migrated off vi.mock; @crm/types made the canonical auth/roles/member/plugin contract (web consumes, api controllers annotated for auth+roles); pipeline.store server-error-message fix; E2E smoke (e2e/, Playwright, 5 tests); coverage radar (~28% baseline); ESLint added to CI test:web. 73 web unit/component tests. ADR-0010 recorded.
Why so:   The dominant vi.mock('../api/client') + toHaveBeenCalledWith pattern is green by construction — see ADR-0010. Chose MSW so store+client+serialization+error-mapping run through the test together; chose to mount real components (useConfirm pattern) for views. Methodology codified in .claude/rules/web.md + .claude/agents/test-writer.md so the /task flow (Stage 3.2 / 5.3) stops reproducing tautologies — that wiring was the load-bearing part, not the individual specs.
Failed:   Nothing reverted, but two things forced a change of plan: (1) P1c — @crm/types already HAD dead, unused, internally-inconsistent User/Role/Permission types (Resource enum 6 values vs PERMISSION_RESOURCES 8); reshaping them beat adding parallel types. (2) API return-type annotation can't be applied to users/plugins controllers: their timestamp columns infer as Date pre-serialization while the web type is string (JSON on the wire) — pinning those needs a Date↔string decision, so deferred. E2E could not be executed locally (no running stack in the sandbox) — validated structurally via `playwright --list`; the e2e:smoke CI job is allow_failure until it proves stable on the runner.
Next:     Follow-ups (candidates for Linear issues):
          1. Flip e2e:smoke off allow_failure once green on the shared runner.
          2. API return-type annotations for users/plugins controllers — decide Date↔string (timestamp mode:'string' vs explicit serialize map), then pin to Member/Plugin.
          3. Deeper E2E: contact CRUD, pipeline drag-and-drop, and a seeded non-super-admin getting 403 on a role mutation (needs per-test data lifecycle + a second user).
          4. Migrate UsersView.spec.ts off vi.mock('../api/client') to MSW (ADR-0010) — landed on main from a parallel session using the old pattern.
          5. Raise coverage off the ~28% baseline; introduce a non-blocking threshold once stable.
Verify:   pnpm verify ✅ (api + web 73 + forms-client 9 + forms-ui 3; typecheck clean incl. vue-tsc + api tsc; lint clean incl. e2e). Marker written by .claude/scripts/verify.mjs. E2E: `playwright --list` = 5 tests (executed in CI, not locally).
