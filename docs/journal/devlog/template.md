# docs/journal/devlog/YYYY-MM-DD-slug.md
Issue:    ABC-00 (Linear) · branch <branch-name>   # mandatory — gives a topic view for free
Goal:     one line: what this session set out to do
Done:     what actually shipped (files/modules; test count)
Why so:   the non-obvious choices — why this path, not the other (link ADR-XXXX if one emerged)
Failed:   what was tried and abandoned, and why (the thing git won't tell you)
Next:     the next handle to pull (out of scope here)
Verify:   pnpm verify ✅/❌ (N tests, typecheck, lint) — paste evidence, never claim
